<Window x:Class="SpotifyMasher.PositionPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="WidthAndHeight"
        Topmost="True"
        Width="280">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{StaticResource BrushSurface}"
            BorderBrush="{StaticResource BrushBorder}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="14,10"
            MinWidth="180"
            MaxWidth="300"
            Cursor="SizeAll"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.5" BlurRadius="12" ShadowDepth="2"/>
        </Border.Effect>
        <Grid>
            <!-- Sample toast text — same font/size/line-height as real toast, gets full width -->
            <TextBlock Text="❤️ Liked&#10;Song Name – Artist"
                       Foreground="{StaticResource BrushText}"
                       FontFamily="Segoe UI Variable"
                       FontSize="13"
                       TextWrapping="Wrap"
                       LineHeight="18"/>
            <!-- ✓ / ✕ overlaid top-right — use TextBlocks so Foreground renders correctly -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right" VerticalAlignment="Top">
                <TextBlock Text="✓"
                           Foreground="{StaticResource BrushAccent}"
                           FontSize="15" FontWeight="Bold"
                           Padding="3,0" Cursor="Hand"
                           MouseLeftButtonDown="DoneText_MouseDown"/>
                <TextBlock Text="✕"
                           Foreground="#FF5555"
                           FontSize="14"
                           Padding="3,0" Cursor="Hand"
                           MouseLeftButtonDown="CancelText_MouseDown"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
