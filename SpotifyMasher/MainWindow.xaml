<Window x:Class="SpotifyMasher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:SpotifyMasher.Controls"
        xmlns:local="clr-namespace:SpotifyMasher"
        Title="Spotify Masher"
        Width="580"
        MinWidth="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="{StaticResource BrushBg}">

    <Window.Resources>
        <Style x:Key="DeleteButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#885555" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="4,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FF6666" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Background watermark logo — adjust these values to taste:
             Width/Height  = size of the image
             Canvas.Left   = horizontal position (negative = bleeds off left edge)
             Canvas.Top    = vertical position (-Height/2 centres it)
             Opacity       = 0.0 (invisible) to 1.0 (full)
             Canvas is zero-size so it never affects window height. -->
        <Canvas Width="0" Height="0" HorizontalAlignment="Left" VerticalAlignment="Center"
                IsHitTestVisible="False">
            <Image Source="/Resources/fuspotify.png"
                   Width="340" Height="340"
                   Opacity="0.15"
                   Canvas.Left="-80" Canvas.Top="-150"
                   RenderOptions.BitmapScalingMode="HighQuality" />
        </Canvas>

        <!-- Main content -->
        <Grid Margin="20,14,20,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />   <!-- Header -->
            <RowDefinition Height="Auto" />   <!-- Auth form (disconnected only) -->
            <RowDefinition Height="Auto" />   <!-- Hotkey section (collapsible) -->
            <RowDefinition Height="Auto" />   <!-- Notification settings (collapsible) -->
            <RowDefinition Height="Auto" />   <!-- Toast Style section (collapsible) -->
            <RowDefinition Height="Auto" />   <!-- Footer -->
            <RowDefinition Height="Auto" />   <!-- Debug log (Ctrl+Shift+` to toggle) -->
        </Grid.RowDefinitions>

        <!-- ══ Header ══ -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Spotify Masher" FontSize="22" FontWeight="Bold"
                           Foreground="{StaticResource BrushAccent}" />
                <TextBlock Text="Global hotkeys for Spotify" FontSize="12"
                           Foreground="{StaticResource BrushTextMuted}" Margin="0,3,0,0" />
            </StackPanel>

            <!-- Connected state -->
            <StackPanel x:Name="AuthConnected" Grid.Column="1"
                        Orientation="Horizontal" VerticalAlignment="Center"
                        Visibility="Collapsed">
                <Ellipse Width="10" Height="10" Fill="{StaticResource BrushAccent}"
                         VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBlock Text="Spotify is connected" FontWeight="SemiBold" FontSize="13"
                           VerticalAlignment="Center" Margin="0,0,12,0" />
                <Button Content="Disconnect" Style="{StaticResource PrimaryButton}"
                        Click="DisconnectButton_Click" />
            </StackPanel>

            <!-- Disconnected state -->
            <StackPanel x:Name="AuthDisconnected" Grid.Column="1"
                        Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="10" Height="10" Fill="#555566"
                         VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBlock Text="Not connected" FontWeight="SemiBold" FontSize="13"
                           Foreground="{StaticResource BrushTextMuted}"
                           VerticalAlignment="Center" Margin="0,0,12,0" />
                <Button Content="Connect" Style="{StaticResource GhostButton}"
                        Click="ConnectButton_Click" />
            </StackPanel>
        </Grid>

        <!-- ══ Auth Form (shown when disconnected) ══ -->
        <Border x:Name="AuthFormSection" Grid.Row="1"
                Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="1" CornerRadius="8"
                Padding="16,12" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="ClientIdBox" Grid.Column="0" Margin="0,0,8,0" />

                    <Button x:Name="AuthButton" Grid.Column="1"
                            Content="Authorise with Spotify"
                            Style="{StaticResource PrimaryButton}"
                            Click="AuthButton_Click" />
                </Grid>

                <TextBlock x:Name="AuthStatusText" Grid.Row="1"
                           Foreground="{StaticResource BrushTextMuted}"
                           FontSize="11" TextWrapping="Wrap">
                    <Run Text="Enter your Spotify Client ID from " /><Hyperlink
                        NavigateUri="https://developer.spotify.com/dashboard"
                        RequestNavigate="DashboardLink_RequestNavigate">developer.spotify.com/dashboard</Hyperlink>
                </TextBlock>
            </Grid>
        </Border>

        <!-- ══ Hotkey section (collapsible) ══ -->
        <!-- The column headers (Keybinding / Action / Parameter) label the content;
             no separate heading needed. Transparent header background prevents corner bleed. -->
        <Grid x:Name="HotkeySection" Grid.Row="2" Margin="0,0,0,10" Visibility="Collapsed">
            <Border BorderBrush="{StaticResource BrushBorder}"
                    Background="{StaticResource BrushSurface}"
                    BorderThickness="1"
                    CornerRadius="8"
                    ClipToBounds="True"
                    MaxHeight="280"
                    Padding="1"
                    Margin="1">
                <DataGrid x:Name="HotkeyGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False">
                    <DataGrid.Columns>

                        <DataGridTemplateColumn Header="Keybinding" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <controls:HotkeyBox BindingTarget="{Binding}"
                                                        Margin="4,3"
                                                        Background="{StaticResource BrushSurface2}"
                                                        Foreground="{StaticResource BrushText}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Action" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{x:Static local:MainWindow.AvailableActions}"
                                              SelectedItem="{Binding Action, UpdateSourceTrigger=PropertyChanged}"
                                              Margin="4,3" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Parameter" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Parameter, UpdateSourceTrigger=PropertyChanged}"
                                             Margin="4,3" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Toast" Width="52">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding ShowToast, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="4,3"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="" Width="36">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="✕" Style="{StaticResource DeleteButton}"
                                            Tag="{Binding}"
                                            Click="DeleteRow_Click" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- ══ Notification Settings (collapsible) ══ -->
        <Border x:Name="NotifSection" Grid.Row="3" Margin="0,0,0,10"
                Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="1" CornerRadius="8"
                Visibility="Collapsed">
            <StackPanel Margin="14">

                    <CheckBox x:Name="NotifEnabled" Content="Enable toast notifications"
                              Checked="NotifEnabled_Checked"
                              Unchecked="NotifEnabled_Unchecked"
                              Margin="0,0,0,10"/>

                    <!-- Position mode selector -->
                    <TextBlock Text="Position" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,6"/>

                    <!-- Mode: Corner + offset -->
                    <RadioButton x:Name="ModeCorner" Content="Corner + offset"
                                 GroupName="PosMode" Checked="ModeCorner_Checked"
                                 Margin="0,0,0,4"/>
                    <Grid x:Name="CornerOffsetPanel" Margin="24,2,0,10">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.35"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Corner:" VerticalAlignment="Center"
                                   Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="0,0,8,0"/>
                        <ComboBox x:Name="NotifCorner" Grid.Column="1"
                                  ItemsSource="{x:Static local:MainWindow.AvailableCorners}"
                                  SelectionChanged="NotifCorner_SelectionChanged"/>
                        <!-- Offset X / Y grouped under one label -->
                        <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                            <TextBlock Text="Offset" VerticalAlignment="Center"
                                       Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="0,0,10,0"/>
                            <TextBlock Text="X:" VerticalAlignment="Center"
                                       Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="0,0,5,0"/>
                            <TextBox x:Name="NotifOffsetX" Width="45" TextAlignment="Right"/>
                            <TextBlock Text="Y:" VerticalAlignment="Center"
                                       Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="8,0,5,0"/>
                            <TextBox x:Name="NotifOffsetY" Width="45" TextAlignment="Right"/>
                        </StackPanel>
                        <!-- Corner indicator: 2×2 dot grid showing active quadrant -->
                        <Border Grid.Column="4" Width="34" Height="34" VerticalAlignment="Center"
                                BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="4">
                            <Grid Margin="4">
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Ellipse x:Name="CornerIndTL" Grid.Row="0" Grid.Column="0" Margin="2"/>
                                <Ellipse x:Name="CornerIndTR" Grid.Row="0" Grid.Column="1" Margin="2"/>
                                <Ellipse x:Name="CornerIndBL" Grid.Row="1" Grid.Column="0" Margin="2"/>
                                <Ellipse x:Name="CornerIndBR" Grid.Row="1" Grid.Column="1" Margin="2"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- Mode: Freehand position -->
                    <RadioButton x:Name="ModeFreehand" Content="Freehand position"
                                 GroupName="PosMode" Checked="ModeFreehand_Checked"
                                 Margin="0,0,0,4"/>
                    <StackPanel x:Name="FreehandPanel" Orientation="Horizontal" Margin="24,2,0,10">
                        <Button Content="Drag to set position" Style="{StaticResource GhostButton}"
                                Click="SetGlobalPosition_Click"/>
                    </StackPanel>

                    <!-- Duration + AlwaysOnTop -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="70"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Duration (ms):" VerticalAlignment="Center"
                                   Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="0,0,8,0"/>
                        <TextBox x:Name="NotifDuration" Grid.Column="1" TextAlignment="Right"/>
                        <CheckBox x:Name="NotifAlwaysOnTop" Grid.Column="2"
                                  Content="Always on top  (borderless windowed games only)"
                                  Margin="16,0,0,0" VerticalAlignment="Center" FontSize="11"/>
                    </Grid>

                    <!-- Per-process overrides -->
                    <TextBlock Text="Per-process overrides" FontSize="12" FontWeight="SemiBold"
                               Margin="0,0,0,4"/>
                    <TextBlock FontSize="11" Foreground="{StaticResource BrushTextMuted}"
                               Margin="0,0,0,6" TextWrapping="Wrap">
                        When this .exe is the foreground window, use a freehand position instead. Click 📍 to drag-set the position.
                    </TextBlock>

                    <Border BorderBrush="{StaticResource BrushBorder}"
                            Background="{StaticResource BrushBg}"
                            BorderThickness="1" CornerRadius="6"
                            ClipToBounds="True" MaxHeight="150" Padding="1" Margin="0,0,0,8">
                        <DataGrid x:Name="ProcessRuleGrid"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Process (.exe)" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding ProcessName, UpdateSourceTrigger=PropertyChanged}"
                                                     Margin="4,3"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="" Width="32">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="📍" Style="{StaticResource GhostButton}"
                                                    Tag="{Binding}" Click="SetProcessRulePosition_Click"
                                                    Padding="2,0" FontSize="13"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="" Width="36">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="✕" Style="{StaticResource DeleteButton}"
                                                    Tag="{Binding}" Click="DeleteProcessRule_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <Button Content="+ Add Rule" Style="{StaticResource GhostButton}"
                            HorizontalAlignment="Left" Click="AddProcessRule_Click"/>
            </StackPanel>
        </Border>

        <!-- ══ Toast Style section (collapsible) ══ -->
        <Border x:Name="StyleSection" Grid.Row="4" Margin="0,0,0,10"
                Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="1" CornerRadius="8"
                Visibility="Collapsed">
            <StackPanel Margin="14">

                <!-- Preset picker (primary control) -->
                <TextBlock Text="Preset" FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,6"/>
                <ComboBox x:Name="StylePreset"
                          ItemsSource="{x:Static local:MainWindow.AvailablePresets}"
                          SelectionChanged="StylePreset_SelectionChanged"
                          Width="220" HorizontalAlignment="Left"
                          Margin="0,0,0,14"/>

                <!-- ══ Aurora Custom — pick your own 4 gradient colours + background ══ -->
                <StackPanel x:Name="AuroraCustomPanel" Visibility="Collapsed" Margin="0,0,0,4">
                    <TextBlock Text="Gradient Colours" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,6"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <controls:ColorPickerBox x:Name="StyleAuroraC1" Width="104" Margin="0,0,6,0"/>
                        <controls:ColorPickerBox x:Name="StyleAuroraC2" Width="104" Margin="0,0,6,0"/>
                        <controls:ColorPickerBox x:Name="StyleAuroraC3" Width="104" Margin="0,0,6,0"/>
                        <controls:ColorPickerBox x:Name="StyleAuroraC4" Width="104"/>
                    </StackPanel>

                    <TextBlock Text="Background Colour" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,6"/>
                    <controls:ColorPickerBox x:Name="StyleAuroraBg" Width="120"
                                             HorizontalAlignment="Left" Margin="0,0,0,4"/>

                    <TextBlock Text="The drifting background shards are derived automatically from your gradient colours."
                               FontSize="10.5" TextWrapping="Wrap"
                               Foreground="{StaticResource BrushTextMuted}" Opacity="0.8"/>
                </StackPanel>

                <!-- ══ Manual controls (hidden — kept for power users / future use) ══ -->
                <StackPanel x:Name="ManualStyleControls" Visibility="Collapsed">

                <!-- Background style (texture) -->
                <TextBlock Text="Background Style" FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,6"/>
                <ComboBox x:Name="StyleBgEffect"
                          ItemsSource="{x:Static local:MainWindow.AvailableBackgroundEffects}"
                          SelectionChanged="StyleBgEffect_SelectionChanged"
                          Width="160" HorizontalAlignment="Left"
                          Margin="0,0,0,12"/>

                <!-- Background colours -->
                <TextBlock Text="Background Colors" FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="Color 1:" VerticalAlignment="Center"
                               Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="0,0,8,0"/>
                    <controls:ColorPickerBox x:Name="StyleBgColor1" Width="160"/>
                    <TextBlock x:Name="StyleBgColor2Label" Text="Color 2:" VerticalAlignment="Center"
                               Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="16,0,8,0"/>
                    <controls:ColorPickerBox x:Name="StyleBgColor2" Width="160"/>
                </StackPanel>

                <!-- Glow colour -->
                <TextBlock Text="Glow Color" FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="Color:" VerticalAlignment="Center"
                               Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="0,0,8,0"/>
                    <controls:ColorPickerBox x:Name="StyleGlowColor" Width="160"/>
                </StackPanel>

                <!-- Text colours -->
                <TextBlock Text="Text Colors" FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="Message:" VerticalAlignment="Center"
                               Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="0,0,8,0"/>
                    <controls:ColorPickerBox x:Name="StyleMsgColor" Width="160"/>
                    <TextBlock Text="Artist:" VerticalAlignment="Center"
                               Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="16,0,8,0"/>
                    <controls:ColorPickerBox x:Name="StyleArtistColor" Width="160"/>
                    <TextBlock Text="Album:" VerticalAlignment="Center"
                               Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="16,0,8,0"/>
                    <controls:ColorPickerBox x:Name="StyleAlbumColor" Width="160"/>
                </StackPanel>

                <!-- Action border -->
                <TextBlock Text="Action Border" FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="Type:" VerticalAlignment="Center"
                               Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="0,0,8,0"/>
                    <ComboBox x:Name="StyleBorderType"
                              ItemsSource="{x:Static local:MainWindow.AvailableActionBorderTypes}"
                              Width="160" Margin="0,0,16,0"/>
                    <TextBlock Text="Color:" VerticalAlignment="Center"
                               Foreground="{StaticResource BrushTextMuted}" FontSize="12" Margin="0,0,8,0"/>
                    <controls:ColorPickerBox x:Name="StyleBorderColor" Width="160"/>
                </StackPanel>

                <!-- Shimmer effect -->
                <TextBlock Text="Shimmer Effect" FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,6"/>
                <ComboBox x:Name="StyleShimmerEffect"
                          ItemsSource="{x:Static local:MainWindow.AvailableShimmerEffects}"
                          Width="160" HorizontalAlignment="Left"
                          Margin="0,0,0,16"/>

                </StackPanel>
                <!-- ══ end Manual controls ══ -->

                <!-- Preview / Reset buttons -->
                <StackPanel Orientation="Horizontal">
                    <Button Content="Preview Toast" Style="{StaticResource GhostButton}"
                            Click="PreviewToast_Click" Margin="0,0,8,0"/>
                    <Button Content="Reset to Defaults" Style="{StaticResource GhostButton}"
                            Click="ResetStyle_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- ══ Footer ══ -->
        <Grid Grid.Row="5" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <!-- When hotkeys collapsed: Hotkeys -->
                <StackPanel x:Name="HotkeyCollapsedButtons" Orientation="Horizontal" Margin="0,0,8,0">
                    <Button Content="Hotkeys" Style="{StaticResource GhostButton}"
                            Click="EditHotkeys_Click"/>
                </StackPanel>
                <!-- When hotkeys expanded: + Add Hotkey + Save -->
                <StackPanel x:Name="HotkeyExpandedButtons" Orientation="Horizontal"
                            Margin="0,0,8,0" Visibility="Collapsed">
                    <Button Content="+ Add Hotkey" Style="{StaticResource GhostButton}"
                            Click="AddHotkey_Click" Margin="0,0,8,0"/>
                    <Button x:Name="SaveButton" Content="Save Hotkeys" Style="{StaticResource PrimaryButton}"
                            Click="Save_Click"/>
                </StackPanel>
                <!-- When notifications collapsed: Notifications -->
                <StackPanel x:Name="NotifCollapsedButton" Orientation="Horizontal">
                    <Button Content="Notifications" Style="{StaticResource GhostButton}"
                            Click="ToggleNotifications_Click"/>
                </StackPanel>
                <!-- When notifications expanded: Save Notifications -->
                <StackPanel x:Name="NotifExpandedButton" Orientation="Horizontal"
                            Visibility="Collapsed">
                    <Button Content="Save Notifications" Style="{StaticResource PrimaryButton}"
                            Click="SaveNotifications_Click"/>
                </StackPanel>
                <!-- When toast style collapsed: Toast Style -->
                <StackPanel x:Name="StyleCollapsedButton" Orientation="Horizontal" Margin="8,0,0,0">
                    <Button x:Name="StyleToggleButton" Content="Toast Style"
                            Style="{StaticResource GhostButton}"
                            Click="ToggleStyle_Click"/>
                </StackPanel>
                <!-- When toast style expanded: Save Style -->
                <StackPanel x:Name="StyleExpandedButton" Orientation="Horizontal"
                            Margin="8,0,0,0" Visibility="Collapsed">
                    <Button Content="Save Style" Style="{StaticResource PrimaryButton}"
                            Click="SaveStyle_Click"/>
                </StackPanel>
            </StackPanel>

            <Button Grid.Column="2" Content="?" Width="30" Height="30" Padding="0"
                    Style="{StaticResource GhostButton}"
                    Click="Help_Click" ToolTip="Help &amp; About" />
        </Grid>

        <!-- ══ Debug Log (Ctrl+Shift+` to toggle, or via tray menu) ══ -->
        <Grid x:Name="DebugSection" Grid.Row="6" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="130" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,4,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Debug Log" FontWeight="SemiBold" FontSize="11"
                           Foreground="{StaticResource BrushTextMuted}" VerticalAlignment="Center" />
                <Button Grid.Column="1" Content="Clear" Click="ClearLog_Click"
                        Style="{StaticResource GhostButton}" Padding="8,2" FontSize="11" />
            </Grid>

            <TextBox x:Name="LogBox" Grid.Row="1"
                     IsReadOnly="True"
                     FontFamily="Consolas" FontSize="10"
                     Background="#0D0D1A"
                     Foreground="#88CC88"
                     BorderBrush="{StaticResource BrushBorder}"
                     BorderThickness="1"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap"
                     Padding="6" />
        </Grid>
        </Grid>
    </Grid>
</Window>
